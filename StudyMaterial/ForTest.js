/**Concat (Array Function) joins multiple arrays together
 */
const a = [1,2,3]
const b = [4,5,6]

const c = a.concat(b)
console.log(c)

/** LOOPS (For, While, Do While ) 
 * Splice (Array Function) is to change an existing array by removing, replacing existin elements from it
EXAMPLE*/

let numbers = [1,2,3,4]
numbers.splice(1,3,897);
console.log(numbers);



/** CharAt(String Function) is to take out specific indexes in a string*/

let sentence = 'Yo whats good bluhh?'
let sentence2 = sentence.charAt(10)
console.log(sentence2)
